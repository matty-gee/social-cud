
**Find task structure in embeddings**
- What features of the task can we decode from the semantic embeddings?
- Can we reconstruct the actual social space from the semantic embeddings?


**Establish a single pipeline for fMRI analyses**
- Model comparison against simpler models
- Model comparison against other equally complex models (esp. social space v. semantic space analyses)
- Ensure the predictor estimates are ~robust to the inclusion/exclusion of the others
- Effect robustness to dropping near-in-time trial pairs
- Effect robustness to dropping characters
- If about character history: 
	- show character-history specificity
	- show effect over different amounts of history


